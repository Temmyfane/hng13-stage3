# Blue/Green Deployment Configuration Template
# Copy this file to .env and update with actual values

# Docker Images - Update these with the actual image URLs from your instructor
BLUE_IMAGE=your-registry/app:blue-tag
GREEN_IMAGE=your-registry/app:green-tag

# Active Pool (blue or green) - Controls which service is primary
ACTIVE_POOL=blue

# Backup Pool (automatically determined, but can be overridden)
# BACKUP_POOL=green

# Release IDs - These will be returned in X-Release-Id headers
RELEASE_ID_BLUE=blue-v1.0.0
RELEASE_ID_GREEN=green-v1.0.0

# Application Port (optional, defaults to 3000)
PORT=3000